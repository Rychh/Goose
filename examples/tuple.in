def triple(k) {
    return (k, k, k);
}

def main() {
   x = (1, 2);
   y = triple(2);
    (a, b) = x;
    println(a);    
    (a, b) = (1,2);
    println(a);

    (e, d, f) =  triple(3);
    println(e);

    return 0;
}