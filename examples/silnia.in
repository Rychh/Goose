def main () {
  printInt(fact(7)) ;
  printInt(factr(7)) ;
  return 0 ;
}

// iteracyjnie
def fact (n) {
  i = 1 ;
  r = 1 ;
  while (i < n+1) {
    r = r * i ;
    i++ ;
  }
  return r ;
}

// rekurencyjnie
def factr (n) {
  if (n < 2) 
    return 1 ;
  else 
    return (n * factr(n-1)) ; 
}